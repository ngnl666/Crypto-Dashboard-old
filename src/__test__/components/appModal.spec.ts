// import { setActivePinia, createPinia } from 'pinia';
// import Dashboard from '@/layouts/Dashboard.vue';
// import AppModal from '@/components/Modals/AppModal.vue';

// describe('AppModal', () => {
// 	beforeAll(() => {
// 		setActivePinia(createPinia());
// 	});

// 	it('modal', async () => {
// 		const wrapper = mount(Dashboard, { props: { headerHeight: 350 } });
// 		await wrapper.find('[data-test="aa"]').trigger('click');
// 		// const aa = wrapper.find('[data-test="bitcoin"]');
// 		// await wrapper.trigger('drag', { button: 0 })
// 		console.log(wrapper.getComponent(AppModal));

// 		expect(1).toBe(1);
// 	});
// });
